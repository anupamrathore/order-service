apiVersion: v1
kind: Service
metadata:
  name: order-service
spec:
  type: NodePort
  selector:
    app: order
  ports:
    - protocol: TCP
      port: 80           # Service port exposed outside
      targetPort: 3003   # Matches containerPort in deployment
      nodePort: 30003    # Optional: manually assign a node port
